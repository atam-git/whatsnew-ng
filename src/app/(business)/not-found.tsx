import { NotFoundContent } from '@/components/business/not-found-content';

// Used for notFound() thrown inside the public site - keeps the header/footer.
export default function BusinessNotFound() {
  return <NotFoundContent />;
}
